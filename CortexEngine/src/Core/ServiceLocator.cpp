#include "ServiceLocator.h"

// Implementation file for service locator
// Currently header-only, but kept for future expansion
namespace Cortex {

// Empty implementation - everything is inline in header

} // namespace Cortex
