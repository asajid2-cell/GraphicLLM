#include "Renderer.h"

namespace Cortex::Graphics {

// Temporal and screen-space render graph entry points are split by pass:
// motion vectors, temporal rejection mask, TAA, SSR, and SSAO.

} // namespace Cortex::Graphics
